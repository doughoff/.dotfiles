general {
	colors = true
}

order += "disk /"
order += "memory"
order += "tztime local"

disk "/" {
    format = "%avail"
}

memory {
    format = "%used"
    threshold_degraded = "10%"
    format_degraded = "MEMORY: %free"
}

tztime local {
    format = "%H:%M"
}